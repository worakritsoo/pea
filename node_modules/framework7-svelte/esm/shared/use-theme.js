import { f7, f7ready, theme } from './f7';
export var useTheme = function useTheme(set) {
  var t = f7 ? theme : null;

  if (!f7) {
    f7ready(function () {
      set(theme);
    });
  }

  return t;
};