export function restProps(rest) {
  if (rest === void 0) {
    rest = {};
  }

  var props = {};
  Object.keys(rest).forEach(function (key) {
    if (key.indexOf('on') !== 0) {
      props[key] = rest[key];
    }
  });
  return props;
}